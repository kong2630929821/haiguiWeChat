<div class="new-page" w-class="page">
    <div w-class="title">确认身份信息</div>
    <div w-class="input">{{it.name}}</div>
    <div w-class="input">{{it.card}}</div>
    <div w-class="input">{{it.validDate}}</div>
    <div w-class="btn" on-tap="verify">确认信息</div>
</div>